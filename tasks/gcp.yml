---

- block:
    - validate:
        schema:
          type: object
          properties:
            command:
              type: string
              enum:
                - control_instance
                - dependency
                - disk_image
                - project_ssh_keys
                - virtual_machine
          required:
            - command
        instance: "{{ gcp }}"

    - include_tasks: '{{ gcp.command }}.yml'
  tags:
    - gcp
